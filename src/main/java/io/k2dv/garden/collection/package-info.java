/**
 * Collection module for curating and merchandising groups of products (e.g., "Summer Sale",
 * "Featured Items"). Collections are either MANUAL (merchants hand-pick members) or AUTOMATED
 * (membership is derived from tag-matching rules evaluated by {@code CollectionMembershipService}).
 * Both types support an ordered product grid and storefront-facing status management.
 */
package io.k2dv.garden.collection;
