/**
 * JPA entities for the gift card module. {@code GiftCard} holds the card code, current and
 * initial balance, expiry, and activation state; {@code GiftCardTransaction} records every
 * credit and debit against a card as an append-only audit trail.
 */
package io.k2dv.garden.giftcard.model;
