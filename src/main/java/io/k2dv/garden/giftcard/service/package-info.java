/**
 * Service layer for the gift card module. Contains {@code GiftCardService}, which manages
 * the full gift card lifecycle including secure code generation, administrative adjustments,
 * and atomic balance deduction at checkout to prevent double-spend.
 */
package io.k2dv.garden.giftcard.service;
