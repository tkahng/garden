/**
 * Service layer for the quote module. {@code QuoteService} orchestrates the full quote
 * lifecycle (submission through order conversion); {@code QuoteCartService} manages the
 * staging cart B2B buyers use before submission; {@code QuotePdfService} generates the
 * customer-facing PDF document from a Thymeleaf template.
 */
package io.k2dv.garden.quote.service;
