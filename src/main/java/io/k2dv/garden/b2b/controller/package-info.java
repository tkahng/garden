/**
 * REST controllers for the B2B module, exposing endpoints under the {@code /api/b2b} prefix.
 * Controllers enforce authentication and company-membership authorization, then delegate
 * all business logic to the corresponding service classes in the sibling service package.
 */
package io.k2dv.garden.b2b.controller;
