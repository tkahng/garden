/**
 * Full-text and faceted search across products, collections, articles, and static pages.
 * Queries are executed against database full-text indexes and composed into a single
 * multi-bucket response so the storefront can display blended search results.
 */
package io.k2dv.garden.search;
