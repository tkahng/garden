/**
 * Service layer for the product catalog domain. {@code ProductService} is the primary entry point
 * for product CRUD, status management, and B2B visibility rules. Specialised services handle
 * variants ({@code VariantService}), option axes ({@code OptionService}), and gallery images
 * ({@code ProductImageService}).
 */
package io.k2dv.garden.product.service;
