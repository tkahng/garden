/**
 * JPA entities for the shipping module. {@code ShippingZone} groups country/province
 * combinations into a named delivery region; {@code ShippingRate} defines a named rate
 * within a zone, including its price, weight bounds, minimum order amount, carrier, and
 * estimated delivery window.
 */
package io.k2dv.garden.shipping.model;
