/**
 * Service layer for the stats module, executing aggregate queries against the order and
 * user repositories and projecting raw database rows into typed DTOs for the dashboard.
 */
package io.k2dv.garden.stats.service;
