/**
 * User wishlist module that lets shoppers save product variants for later purchase.
 * A wishlist is created on demand for each user and persisted so saved items survive
 * across sessions and devices.
 */
package io.k2dv.garden.wishlist;
