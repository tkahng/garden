/**
 * Admin sub-module for user management: provides filterable user search, profile editing,
 * account suspension and reactivation, bulk status operations, tag and metadata management,
 * and role assignment. Exposes a CSV export endpoint for offline reporting.
 */
package io.k2dv.garden.admin.user;
